plugins {
    // Compose compiler plugin also pulls Kotlin 2.4.10 onto the build
    // classpath, overriding AGP 9.3's bundled KGP 2.2.10.
    id 'com.android.application' version '9.3.1' apply false
    id 'org.jetbrains.kotlin.plugin.compose' version '2.4.10' apply false
}
